/**
 * this file is technically tested by all the other files
 * so only specific modications are in here
 * @see remote.ts for consumption of stuff in ./dsl
 */
import * as dsl from '#utils/groovy/dsl'

describe('dsl', () => {
  describe('GroovyTraversal', () => {
    describe('keys', () => {
      test.skip('is chainable', async () => {
        expect(false).toBeTrue()
      })
    })

    describe('next<T>', () => {
      test.skip('typescript returns as T', () => {
        expect(false).toBeTrue()
      })
    })
    describe('nextMap<T>', () => {
      test.skip('typescript returns as TraverserMap<T>', () => {
        expect(false).toBeTrue()
      })
    })
  })

  describe('anonymous', () => {
    describe('keys', () => {
      test.skip('is chainable', () => {
        expect(false).toBeTrue()
      })
    })
  })
})
